package com.example.validation.domain.service;

import org.springframework.stereotype.Service;

@Service
public class TempService {

    public boolean exists(boolean returned) {
        return returned;
    }

}
